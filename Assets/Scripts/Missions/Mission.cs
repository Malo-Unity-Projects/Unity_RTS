using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Mission : MonoBehaviour
{
    public enum MissionType { DEFENSE, SURVIVAL, ANNIHILATION, DESTRUCTION, CONQUEST };
    public MissionType missionType;

    /* Un seul script pour tous les types de mission ou un script pour chaque type de mission? */

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
